import { EmptyState } from '@/components/admin/ui';

export default function AdminSettingsPage() {
  return (
    <EmptyState
      title="Settings"
      description="Phase 4 will expose delivery fee, app name, support phone and more."
    />
  );
}

