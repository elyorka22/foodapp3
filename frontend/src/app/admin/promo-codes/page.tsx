import { EmptyState } from '@/components/admin/ui';

export default function AdminPromoCodesPage() {
  return (
    <EmptyState
      title="Promo Codes"
      description="Phase 5 will add promo code CRUD, limits, expiration and enable/disable."
    />
  );
}

